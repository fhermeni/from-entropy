#!/bin/sh

JAVA_OPTS="-mx4G -ms4G"
#Define the classpath
JARS=`ls jar/*.jar`

for JAR in $JARS; do
 CLASSPATH=$JAR:$CLASSPATH
done

java $JAVA_OPTS -cp $CLASSPATH btrplace.benchLauncher.SeqLauncher $*
